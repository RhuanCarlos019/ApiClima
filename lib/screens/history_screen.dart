import 'package:flutter/material.dart';

class TelaHistorico extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Histórico de Consultas'),
      ),
      body: Center(
        child: Text('Página de histórico de consultas'),
      ),
    );
  }
}
